[project]
name = "Japanese-Study"
version = "0.1.0"
description = "Scripts to process Japanese learning materials"
requires-python = ">=3.9"

dependencies = [
    "pyyaml",
    "pykakasi"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
